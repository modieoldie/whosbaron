# CLAUDE.md

## Testing / running the app

**Do not run `npm run dev`, `npm run build`, `npm run preview`, or any other dev/serve command.**

The user already has the dev server running on localhost. After making a change, just
state what changed and what to look at — the user verifies visually in the browser and
reports back any issues.
